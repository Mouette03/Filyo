# Configuration Filyo
# Copiez ce fichier en .env et ajustez les valeurs

# Port exposé sur l'hôte (défaut: 80)
PORT=80

# URL publique du frontend
FRONTEND_URL=http://localhost

# Chemin local pour stocker les données (DB + uploads)
DATA_PATH=./data

# Niveau de log backend (trace | debug | info | warn | error)
LOG_LEVEL=info

# Clé secrète JWT — CHANGEZ impérativement en production !
JWT_SECRET=change-this-to-a-long-random-secret
